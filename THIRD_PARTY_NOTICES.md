# Third-Party Notices

`moonview` source is licensed under Apache-2.0.

The Windows backend compiles against the Microsoft WebView2 SDK and uses the
installed Microsoft Edge WebView2 Runtime. Those components are not relicensed
by this repository and are not bundled in it.

The planned macOS and Linux backends will use system WKWebView and WebKitGTK
respectively. Any future bundled binary or copied upstream source must add its
applicable license and notice here before release.
